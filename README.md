App for patient prescription visualisation
=======================

### Requirements

- Install Docker
- Install Docker Compose

### How to run

- docker-compose up --build -d

To get containers details:

- docker-compose ps
